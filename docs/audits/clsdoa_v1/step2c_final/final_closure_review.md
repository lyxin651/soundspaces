# ClassDOA V1 Step 2C Final Closure Regression

This regression was run from the clean closure state `31928fe7bdf651360552db3e1d4a1ac0e778f351`. The production renderer origin remains `4dd94a4b06977acaecdb0ba9af7ab7a7ba2ca78d`; the integrated generation code is `552c299fa14d355f877eca7ff068154dcc463c21`. These identities differ by design: the first identifies the approved renderer scientific implementation, while the second identifies the approved combined generation state. The source payload generation identity remains `d55153ffab31fd1f9841fc65bb5ae0909fa58a55`, not the later source hardening commit.

The regression used the existing production contract harness and wrote temporary WAV/RIR payload only below `/tmp/clsdoa-step2c-final-regression-20260829`. Production Binaural, FOA live rendering, paired immutable-recipe checks, receiver invariant, sequential lifecycle, temporal placement, gain ratio, no-normalization, finite/nonzero and channel-shape checks all passed. The Binaural channel order is LEFT/RIGHT. P0-B Golden passed for the six cardinal cases and `off_axis_yawed`; current model-facing maximum angular error was `3.078324659199975e-06` degrees. No 103-scene admission, 206-probe revalidation, 960 PLAN, pilot render, or training was executed.

The final resource lock rehashes source registry `f7a59a7e245a8ae0016e5fbd47a5599c64627951e344fe049fef3ff48bab5ad1` and scene registry `06f90a902e78bb26d0e23fdb267bebde55f56d0abe794ecaf763a172784b9508`, matching the Controlled Integration candidates. It records 422 sources with zero source identity leakage and 108 scenes with 103 PASS, 5 geometry FAIL, split-v2 unchanged, and zero scene overlap. The lock points to the Step 2B authority pointer and only its exact-production evidence; floor-receiver evidence, `954deee8d3ce5cab070c436deea91118a416977d`, and old four-outlier evidence remain historical/superseded and are explicitly excluded.

Runtime provenance has no drift: Habitat-Sim is `0.2.2`, RLRAudioPropagation is fingerprinted by its installed path, size `7327344`, and SHA `31e948eef4908d8cbb403b5f445d9d0eab59fc81b05a658538f8795984f9bfb4`. HRTF has no independently exposed installed asset path or hash, so the lock records `NOT_EXPOSED_BY_INSTALLED_HABITAT_SIM` and fingerprints the enclosing implementation instead. The frozen acoustic contract remains 24 kHz, 5 seconds, 5000/200 rays, Materials OFF, receiver `listener_base + [0,1.5,0]`, native N3D to ACN/SN3D FOA, sequential lifecycle, and no per-render or per-viewpoint normalization.

The Final Closure Regression is PASS with no registry, code-contract, or runtime drift detected. This completes Step 2 provenance closure, but Step 3 Pilot Readiness remains pending human review.
